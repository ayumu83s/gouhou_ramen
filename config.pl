#!/usr/bin/env perl

use strict;
use warnings;
use Config::Pit;

Config::Pit::set("OAuth", data => +{
	consumer_key => 'xxx',
	consumer_secret => 'xxx',
	access_token => 'xxx',
	access_token_secret => 'xxx',
});

